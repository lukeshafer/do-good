'use strict';

/**
 *  featured-fundraiser controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::featured-fundraiser.featured-fundraiser');
