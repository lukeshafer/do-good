'use strict';

/**
 * featured-fundraiser service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::featured-fundraiser.featured-fundraiser');
